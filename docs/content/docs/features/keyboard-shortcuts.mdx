---
title: Keyboard Shortcuts
description: Every global keyboard shortcut, in one place
---

{/* Single source of truth: this file is rendered as-is here, and its body
     (frontmatter stripped) is imported raw and rendered via react-markdown
     in the app's Settings dialog — see lib/shared-docs.ts. Keep every bullet
     as `key` (description) so both renderers can style the key consistently,
     and every group label as a `###` heading (styled by the modal's own
     KEYBOARD_SHORTCUTS_MARKDOWN_COMPONENTS h3 mapping). */}

### Visualization modes

- `Shift` (tap alone, either side, Terrain mode only) — toggle the Raster Basemap on/off, without opening the sidebar.
- `Ctrl` (tap alone, either side, Terrain mode only) — hide every visualization mode down to just the plain basemap; tap again to restore whichever modes were on.
- `Space` — re-toggle whichever visualization-mode checkbox you last clicked, even after a map drag has moved keyboard focus onto the map canvas.
- `L` (hold, Terrain mode only) + drag — set the Hillshade illumination direction/altitude directly on the map instead of panning it; release L or the mouse to exit.

### Navigation and UI

- `Ctrl` + `K` — jump focus to the search box (geocoder) from anywhere.
- `←` / `→` (after editing a dropdown) — cycle through that dropdown's options without reopening it.

### Historical Timeline

- `←` / `→` (after picking a date) — step that view's picked date one mark at a time.
- `Ctrl` + drag (timeline handle) — also sweeps every other handle on that same side along with it, by the same number of marks.

### Drawing

- `Esc` — bail out of whatever drawing tool is active (point, polygon, circle…) back to Select.

With a layer's **Feature Iterator** open (the loop toggle on a layer row — see [Tools → Drawing](/features/tools#drawing)), and focus outside a text field:

- `→` or `N` — frame the next feature (wraps around at the end).
- `←` — frame the previous feature.
- `D` / `Delete` / `Backspace` — delete the currently framed feature, then frame the next one.
